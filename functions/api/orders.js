function json(data,status=200){return new Response(JSON.stringify(data),{status,headers:{"Content-Type":"application/json"}})}
export async function onRequestGet(context){try{const DB=context.env.DB;await DB.prepare(`CREATE TABLE IF NOT EXISTS orders (order_no TEXT PRIMARY KEY, data TEXT NOT NULL, updated_at TEXT NOT NULL)`).run();const rows=await DB.prepare(`SELECT data FROM orders ORDER BY order_no`).all();return json({ok:true,orders:(rows.results||[]).map(r=>JSON.parse(r.data))})}catch(e){return json({ok:false,error:e.message},500)}}
export async function onRequestPost(context){try{const DB=context.env.DB;const body=await context.request.json();const orders=body.orders||[];await DB.prepare(`CREATE TABLE IF NOT EXISTS orders (order_no TEXT PRIMARY KEY, data TEXT NOT NULL, updated_at TEXT NOT NULL)`).run();for(const o of orders){await DB.prepare(`INSERT OR REPLACE INTO orders (order_no,data,updated_at) VALUES (?,?,?)`).bind(String(o.order_no),JSON.stringify(o),new Date().toISOString()).run()}return json({ok:true,saved:orders.length})}catch(e){return json({ok:false,error:e.message},500)}}
